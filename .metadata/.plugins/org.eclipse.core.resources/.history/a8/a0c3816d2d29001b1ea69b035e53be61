#include "main.h"
#include "sapi.h"
#include "led.h"
#include "semaforo.h"

uint16_t tiempo = 0;

int main(void)
{
	boardInit();
	while (1)
	{
		tiempo = tickRead();
		if(tiempo >= 100){
			Toggle(VERDE);
			tickWrite(0);
		}
	}

}
